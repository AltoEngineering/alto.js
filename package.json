{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/node": "^12.0.7",
    "lerna": "^3.14.1",
    "typescript": "^3.5.1"
  },
  "scripts": {
    "publish": "lerna run tsc && lerna publish"
  }
}
